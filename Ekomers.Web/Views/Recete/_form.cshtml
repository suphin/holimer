@model ReceteVM
@{
    var parametreler = ViewBag.ReceteParametreListe;
    var seciliParametreIdler = Model.ReceteParametreDegerListe?.Select(x => x.ParametreID).ToList() ?? new List<int>();
}
<div class="form-group row">
    
    <div class="col-lg-6">
        <partial name="~/Views/Malzeme/_selectList.cshtml" model="Model?.Malzeme ?? new Malzeme()" />
    </div>  
    <div class="col-lg-3">
        
    </div>
    <div class="col-lg-3">
       
    </div> 
  
</div>
<div class="form-group row">
    <div class="col-lg-12">
        <label>Üretim Parametreleri</label>
        <div>
         @*    @foreach (ReceteParametre parametre in parametreler)
            {
                <div class="form-check">
                    <input type="checkbox"
                           name="SeciliParametreler"
                           value="@parametre.ID"
                           class="form-check-input"
                           id="parametre_@parametre.ID" />
                    <label class="form-check-label" for="parametre_@parametre.ID">
                        @parametre.Ad
                    </label>
                </div>
            } *@
            @foreach (var parametre in parametreler)
            {
                var isChecked = seciliParametreIdler.Contains(parametre.ID);

                <div class="form-check">
                    <input type="checkbox"
                           name="SeciliParametreler"
                           value="@parametre.ID"
                           class="form-check-input"
                           id="parametre_@parametre.ID"
                           @(isChecked ? "checked" : "") />
                    <label class="form-check-label" for="parametre_@parametre.ID">
                        @parametre.Ad
                    </label>
                </div>
            }
        </div>
    </div>
</div>


<div class="form-group row">
    <div class="col-lg-6">
    @{
           // await Html.RenderPartialAsync("~/Views/Musteriler/_selectList.cshtml", Model?.Musteri ?? new Musteriler());
    }
         
    </div>
    <div class="col-lg-3">
  
    
        
    </div>
    <div class="col-lg-3">
      
        
    </div>
    
    

</div>

 
 
 
 

 
<div class="form-group row">
    <div class="col-lg-12">
        <label asp-for="Not" class="control-label"></label>
        <textarea asp-for="Not" type="text" class="form-control form-control-sm" rows="2" required></textarea>
        <span asp-validation-for="Not" class="text-danger"></span>
    </div>
</div>
@if (Model.ID>0)
{
  @*  <div class="form-group row">
        <div class="col-lg-12">
            <button onclick="UrunSeciniz('@Model.ID')" class="btn btn-sm btn-info font-weight-bold" type="button">Ürün Seçiniz</button>
        </div>
    </div> *@
    <div id="UrunlerDiv">

    </div>
   
}

<script src="js/select2.js?v=7.0.5"></script>




 
 